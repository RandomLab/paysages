
function setup() {
    createCanvas(window.innerWidth, window.innerHeight);
}
function draw() {

    fill("blue");
    rect(0, 0, canvas.width, canvas.height);

    fill("green");
    rect(0, 300, canvas.width, canvas.height);

    fill("yellow");
    circle(200,100,100);

}

